## IDF Component Manager Manifest File
dependencies:
  idf:
    version: ">=5.0"
  esp-box:
    version: "=3.0.5"
    rules:
      - if: target in [esp32s3]
  esp_rainmaker:
    version: "^1.2.0"
  extended_vfs:
    version: "^0.3.1"
  wasm-micro-runtime:
    version: "^1.3"

  joltwallet/littlefs:
    version: ^1.6.0