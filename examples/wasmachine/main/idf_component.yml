## IDF Component Manager Manifest File
dependencies:
  idf:
    version: ">=5.0"
  esp-box:
    version: "=3.0.5"
    rules:
      - if: target in [esp32s3]

  joltwallet/littlefs:
    version: "==1.*"

  wasmachine_core:
    version: "==0.*"
    override_path: "../../../components/wasmachine_core"